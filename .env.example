# Example environment file for IA providers
# Copy to .env and fill secrets locally. direnv will load it automatically.

# OpenAI (conversational)
OPENAI_API_KEY=
# Optional model (default: gpt-4o-mini)
OPENAI_MODEL=gpt-4o-mini

# Moonshot (complex tasks) - OpenAI-compatible API
MOONSHOT_API_KEY=
# e.g. https://api.moonshot.ai/v1
MOONSHOT_BASE_URL=
# Optional model (default: moonshot-large)
MOONSHOT_MODEL=moonshot-large